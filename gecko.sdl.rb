s_

[3.5] | {
       on.program user(det) call rep('apt') on funcTerm exclude(db-md) 
       read apt replace(as_term) = "gecko" 
       for.user call dependant pass_form on:exec 
}

[3.5] | {
       apt_func read *as (prev_rollback) continue func.exec_comp
       const:pool(by_active_con) read *as pm(func.exec:still) replacec.det(call) apt = gecko_(convert)
}

